# Copy to .env and fill values (never commit real secrets)

# OpenAI (used for embeddings during ingestion and optional semantic search)
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional: change embedding model in code if desired
# EMBEDDING_MODEL=text-embedding-3-small

# FastMCP server
FASTMCP_HOST=0.0.0.0
FASTMCP_PORT=8000
FASTMCP_PATH=/mcp

# If using OAuth in production, this must be your public base URL, e.g. https://your-service-xyz.a.run.app
# FASTMCP_BASE_URL=

# OAuth 2.0 (RemoteAuthProvider) - enable only if you have an IdP + JWT settings
AUTH_ENABLED=false
# AUTH_JWKS_URI=https://YOUR_IDP/.well-known/jwks.json
# AUTH_ISSUER=https://YOUR_IDP
# AUTH_AUDIENCE=YOUR_API_AUDIENCE
# AUTHORIZATION_SERVERS=https://YOUR_IDP
# AUTH_ALLOWED_REDIRECT_URIS=https://*,http://localhost:*
