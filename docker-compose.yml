services:
  mcp-server:
    build:
      context: .
      dockerfile: services/mcp_server/Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATA_DIR=/app/data
      - FASTMCP_HOST=0.0.0.0
      - FASTMCP_PORT=8000

